# PRD — 종우's 뉴스레터 에이전트

| 항목 | 내용 |
|---|---|
| 문서 버전 | v1.4 (소스 2분할 레이아웃·증인 풀 확장·엔티티 질의 정밀화·저작권 라이선스) |
| 작성일 | 2026-09-15 |
| 코드네임 | `newsagent` |
| 런타임 | Python 3.14 / LangGraph 1.2 / Flask Local GUI |
| 저작권자 | cjwmanelf (All Rights Reserved) |

> v1.1 — 구현·실측으로 확정한 설계 결정: 2단계 키워드 IDF 가중과 벡터 정규화(3.2),
> 소스 `role` 구분(3.1), 4단계 벡터 표현 교체와 사건별 증인 검색(3.4).
>
> v1.2 — 검증 신뢰성 보강: 증인 본문 실제 확보와 검색 백엔드 이원화(3.4),
> 증인 시효 검사, 통신사 전재 탐지. 발행 이력 기반 중복 방지(3.6),
> 체크포인트·재시도·발행 승인(2.4). 근거 수치는 9장.
>
> v1.3 — 로컬 GUI(3.7), 발행 채널 다중화(3.5), LLM 백엔드에 Gemini·로컬 서버 추가(4).
>
> v1.4 — 교차검증 풀 및 정밀도 강화: GUI 소스 2분할(본문 24개 / 증인 11개) 레이아웃 및 선수-심판 분리 원칙 확립(3.1),
> 고정밀 엔티티·하드웨어 모델·부제 분리 검색 쿼리 엔진 고도화(3.4),
> 내장 자동 실행 스케줄러(CLI/GUI) 및 단독 기획 보도 판정 실측 데이터 검증(3.4, 9장),
> 독점적 저작권 라이선스(`LICENSE`, cjwmanelf) 명시.


---

## 1. 개요

### 1.1 배경
관심 주제의 뉴스를 매일 사람이 훑는 대신, 수집부터 사실 검수·발행까지를 LangGraph 파이프라인으로 자동화한다.
기존 "RSS → 요약 → 전송" 류 도구와의 차별점은 **4단계 검수(교차 출처 사실 확인)** 이다.
단일 기사만 보고 요약하면 오보·낚시성 기사를 그대로 증폭시키므로,
같은 사건을 다룬 **독립 출처 몇 곳이 같은 사실을 말하는지**로 신뢰도를 매긴다.

### 1.2 목표
- 한 번의 실행(`python run.py`)으로 수집 → 발행까지 완주한다.
- 각 단계는 독립 교체 가능한 모듈이며, 설정 파일만 고쳐도 소스·관심사·발행처를 바꿀 수 있다.
- 발행되는 모든 항목에 **신뢰도 등급과 근거 출처 수**가 표시된다.

### 1.3 비목표 (v1 범위 밖)
- 다중 사용자·구독자 관리 — 1인용 로컬 도구다
- 원격 배포 — GUI 는 127.0.0.1 전용. 로컬 파일과 .env 비밀값을 다루므로 외부에 열지 않는다
- 이메일 발행 (v1.3 에서 디스코드·슬랙·텔레그램까지 구현)
- 실시간 스트리밍 — 배치 실행만 하고 스케줄링은 OS 작업 스케줄러에 위임

---

## 2. 시스템 아키텍처

### 2.1 LangGraph 그래프

```
          ┌─────────┐
START ──► │ collect │  1. 수집
          └────┬────┘
               ▼
          ┌─────────┐
          │ curate  │  2. 선별
          └────┬────┘
               │
        selected == 0 ?
         ├─ yes ───────────────────► END  (no-op 리포트)
         └─ no
               ▼
          ┌──────────┐
          │ research │  3. 취재
          └────┬─────┘
               ▼
          ┌─────────┐
          │ verify  │  4. 검수
          └────┬────┘
               ▼
          ┌─────────┐
          │ publish │  5. 발행
          └────┬────┘
               ▼
              END
```

- 노드 간 전달은 단일 `NewsletterState` (TypedDict).
- 각 노드는 **부분 상태만 반환**하고 LangGraph가 머지한다.
- 노드 내부 예외는 잡아서 `state["errors"]`에 누적 — 한 소스/한 기사의 실패가 전체 실행을 죽이지 않는다.
- 조건부 엣지는 `curate` 뒤 한 곳. 선별 결과가 0건이면 LLM 호출 없이 즉시 종료한다(비용 방어).

### 2.4 운영 기능

| 기능 | 적용 |
|---|---|
| 체크포인트 | SQLite 에 실행 상태를 남긴다. 발행만 실패한 경우 수집·요약을 다시 돌리지 않고 재개(`--resume`) |
| 노드 재시도 | `collect` 에만 건다. 아래 표 참고 |
| 발행 승인 | `--approve` 로 `publish` 직전에 멈추고 사람 확인을 받는다 |
| 비용 관측 | LLM 호출 수·입출력 토큰을 집계해 실행 요약에 출력 |

**재시도를 노드마다 다르게 두는 이유**

| 노드 | 재시도 | 근거 |
|---|---|---|
| `collect` | 2회 | 네트워크 실패가 실제로 잦고, 다시 돌려도 HTTP GET 뿐이라 안전하다 |
| `curate` | 없음 | 순수 계산이라 재시도할 이유가 없다 |
| `research`·`verify` | 없음 | 노드 안에서 기사별 실패를 이미 격리한다. 노드째 재시도하면 이미 성공한 기사까지 LLM 을 다시 호출해 비용만 두 배가 된다 |
| `publish` | **금지** | 전송 도중 끊기면 같은 글을 두 번 올릴 수 있다. 레이트리밋 재시도는 `DiscordPublisher` 안에서 멱등하게 처리한다 |

### 2.2 상태 스키마

```python
class NewsletterState(TypedDict):
    run_id: str                    # 실행 식별자 (타임스탬프 기반)
    started_at: str
    raw_items: list[Article]       # 1단계 산출: 중복 제거된 기사 후보
    selected: list[Article]        # 2단계 산출: 관심사 통과 기사
    corpus: list[Article]          # 2단계 산출: 검수용 대조군(탈락 기사 포함 전체)
    briefs: list[Brief]            # 3단계 산출: 요약 + key_facts
    verified: list[VerifiedBrief]  # 4단계 산출: 검수 등급 부여
    publish_result: dict           # 5단계 산출
    errors: list[str]
    stats: dict                    # 단계별 건수 / 소요시간
```

### 2.3 데이터 모델

| 모델 | 필드 |
|---|---|
| `Article` | `id`(정규화 URL 해시), `title`, `url`, `source`, `domain`, `source_type`(rss/api/crawl), `role`(content/corroboration), `published_at`, `snippet`, `content`(3단계에서 채움), `score`, `keyword_score`, `vector_score`, `matched_interest`, `matched_keywords` |
| `Brief` | `article_id`, `headline`, `summary`(bullet 3~5), `key_facts`(검증 대상 원자 문장), `entities`, `category`, `why_it_matters` |
| `VerifiedBrief` | `brief`, `verdict`, `confidence`(0~1), `corroborating_sources`(도메인 리스트), `cluster_size`, `fact_checks`, `notes` |

`verdict` 값: `VERIFIED` / `LIKELY` / `SINGLE_SOURCE` / `DISPUTED`

---

## 3. 단계별 상세 명세

### 3.1 [1단계] 수집 — `collect`

**목적** 설정된 소스에서 최근 기사 후보를 모은다.
**입력** `config/sources.yaml`

**수집기 3종** — 공통 인터페이스 `Collector.collect() -> list[Article]`

| 타입 | 구현 | 주요 설정 키 |
|---|---|---|
| `rss` | `feedparser`. RSS 2.0 / Atom 1.0을 동일 인터페이스로 흡수 | `url` |
| `api` | 범용 JSON API. 응답 경로와 필드명을 매핑으로 지정 → NewsAPI·GDELT·자체 API 모두 대응 | `url`, `params`, `headers`, `items_path`, `field_map` |
| `crawl` | `requests` + `BeautifulSoup`. 목록 페이지에서 링크 추출 → 기사 메타 수집 | `url`, `list_selector`, `title_selector`, `link_attr`, `base_url` |

**소스 역할 (`role`) 및 2분할 구조**
- GUI 콘솔에서 **📰 본문 뉴스 소스(24개)** 와 **🛡️ 증인 전용 소스(11개)** 의 2분할 컬럼으로 완벽히 분리되어 관리된다.

| 값 | 의미 | 관리 대상 |
|---|---|---|
| `content` (기본) | 선별·요약 대상이자 교차검증 증인 | IT 전문지, 신문사, 테크 블로그, 연합뉴스 경제/산업 등 24개 소스 |
| `corroboration` | 교차검증 증인으로만 사용. 뉴스레터 본문에는 실리지 않음 | 구글뉴스(5개 토픽), 연합뉴스(속보/경제 2개), Yahoo Finance 등 11개 소스 |

뉴스 애그리게이터(구글뉴스 등)는 `corroboration` 으로 둔다. 링크가 암호화 리다이렉트라
원문 본문을 가져올 수 없어 3단계 요약이 제목 재탕이 되기 때문이다.
대신 한 사건을 다룬 매체를 폭넓게 끌어오므로 4단계 증인으로는 가장 값지다.

**선수와 심판의 분리 원칙 (검증 무결성 보장)**:
- 동일 언론사가 본문(`content`)과 증인(`corroboration`) 양쪽에 존재하더라도(예: 연합뉴스), 4단계 검수 시 **`candidate.domain == target.domain` 및 바이라인 전재 배제 규칙**에 따라 자기 자신을 증인으로 삼는 행위는 100% 원천 차단된다.
- 즉, 특정 기사에 따라 연합뉴스는 '선수(본문)'로 뛰거나 반대로 '심판(증인)'으로만 뛰도록 철저히 격리되어 검증 객관성이 조금도 희석되지 않는다.

**요구사항**


| ID | 내용 |
|---|---|
| R1-1 | 소스별 `enabled` 플래그로 켜고 끈다 |
| R1-2 | 소스 수집은 스레드 풀로 병렬 처리하고 소스당 타임아웃(기본 15초)을 건다 |
| R1-3 | 한 소스의 실패는 경고로 기록하고 나머지는 계속한다 |
| R1-4 | `lookback_hours`(기본 24) 밖의 기사는 버린다. 발행시각 파싱 실패 시 **보수적으로 유지**한다 |
| R1-5 | 중복 제거 — URL에서 `utm_*`·`fbclid` 등 추적 파라미터를 제거·정규화해 해시. 제목 중복 판정은 **같은 도메인 안에서만** 한다. 매체가 다른데 제목이 같은 기사는 중복이 아니라 4단계가 찾는 독립 출처다 |
| R1-6 | User-Agent를 명시하고, 크롤 소스는 `robots.txt`를 확인해 차단된 경로는 건너뛴다 |
| R1-7 | 소스별 `max_items` 상한 (기본 50) |

**산출** `raw_items`

---

### 3.2 [2단계] 선별 — `curate`

**목적** 관심사에 맞는 기사만 남긴다. **LLM을 쓰지 않는다** (비용·속도 방어선).
**입력** `config/interests.yaml`

```yaml
mode: hybrid          # keyword | vector | hybrid
threshold: 0.35
max_articles: 8
weights:
  keyword: 0.6
  vector: 0.4
interests:
  - name: AI 인프라
    weight: 1.0
    keywords: [GPU, 추론, 데이터센터, NPU, HBM]
    exclude: [루머, 유출]
```

**점수 계산**

1. **키워드 점수** — 제목 매칭은 ×2, 스니펫 매칭은 ×1. 여기에 **키워드별 IDF 가중**을 곱한다.
   IDF 는 그날 수집한 코퍼스에서 직접 계산한다. `AI`·`인공지능` 처럼 후보 절반에 등장하는
   단어는 관심사 일치의 근거가 못 되고, `HBM`·`TSMC` 는 등장 자체가 강한 신호다.
   같은 1점으로 세면 범용어 두 개가 박힌 보도자료가 특종을 이긴다(9장 참고).
2. **벡터 점수** — 관심사마다 `name + keywords`를 이어붙인 프로필 문서를 만들고, 기사(`title + snippet`)와 **TF-IDF 코사인 유사도**를 계산한다. 한국어 대응을 위해 **문자 n-gram(2~4)** 분석기를 쓴다 — 형태소 분석기 의존성 없이 조사·어미 변화를 흡수한다.
   코사인 원값은 0.01~0.11 에 몰려 그대로 쓰면 기여도가 0.04 수준이라, **관심사(열)별 최대값으로 나눠 정규화**한다.
   전체를 한 기준으로 나누면 프로필이 긴 관심사가 구조적으로 유리해지므로 반드시 열 단위로 한다.
3. **최종 점수** = `w_kw × keyword + w_vec × vector`. 관심사별 점수 중 최댓값을 채택한다.

**요구사항**

| ID | 내용 |
|---|---|
| R2-1 | `exclude` 키워드가 제목/스니펫에 있으면 점수와 무관하게 탈락(하드 필터) |
| R2-2 | `threshold` 이상만 남기고 점수 내림차순 상위 `max_articles`건으로 자른다 |
| R2-3 | 탈락 기사도 버리지 않고 `corpus`에 보존한다 — **4단계 교차검증의 대조군** |
| R2-4 | 각 기사에 `score`·`matched_interest`·`matched_keywords`를 기록해 사후 튜닝을 가능하게 한다 |
| R2-5 | `mode`로 키워드 전용 / 벡터 전용 / 하이브리드를 전환한다 |
| R2-6 | `role=corroboration` 소스의 기사는 점수는 매기되 선별 대상에서 제외한다 |

**산출** `selected`, `corpus`

---

### 3.3 [3단계] 취재 — `research`

**목적** 선별된 기사의 본문을 확보하고 구조화 요약을 만든다.

**흐름**

1. **본문 추출** — 기사 HTML을 가져와 `script/style/nav/aside/footer`를 제거하고, `<article>` → `<main>` → 텍스트 밀도가 가장 높은 `<div>` 순으로 본문 후보를 고른 뒤 `<p>` 텍스트를 잇는다. 실패 시 피드 `snippet`으로 폴백.
2. **LLM 요약** — 본문을 넣고 JSON 구조화 출력을 받는다.

```json
{
  "headline": "한 줄 제목 (한국어)",
  "summary": ["핵심 1", "핵심 2", "핵심 3"],
  "key_facts": ["검증 가능한 원자적 사실 문장", "..."],
  "entities": {"org": [], "person": [], "product": []},
  "category": "정책|기업|기술|시장|기타",
  "why_it_matters": "왜 중요한지 1~2문장"
}
```

**요구사항**

| ID | 내용 |
|---|---|
| R3-1 | `key_facts`는 **하나의 주장만 담은 검증 가능한 문장**이어야 한다(수치·주체·행위 포함). 4단계의 입력이므로 품질이 핵심 |
| R3-2 | 기사별 병렬 처리(기본 동시 4건), 개별 실패는 스킵하고 `errors`에 기록 |
| R3-3 | 본문은 상한(기본 12,000자)으로 자른다 |
| R3-4 | JSON 파싱 실패 시 1회 재시도, 그래도 실패하면 스니펫 기반 최소 Brief로 폴백 |
| R3-5 | 원문 언어와 무관하게 출력은 한국어 |

**산출** `briefs`

---

### 3.4 [4단계] 검수 — `verify`

**목적** "여러 독립 출처가 같은 말을 하면 사실로 본다"는 교차검증 원칙으로 신뢰도를 매긴다.

**흐름**

0. **같은 사건 병합** — 선별된 기사들끼리 같은 사건이면 점수가 높은 쪽만 남긴다.
   같은 소식이 뉴스레터에 두 번 실리는 것을 막는다. 밀려난 기사는 버려지지 않고
   `corpus`에 남아 대표 기사의 증인으로 다시 잡힌다.
1. **증인 후보 수집** — 두 곳에서 모은다.
   - `corpus` 전체(선별 통과 + 탈락)
   - **사건별 뉴스 검색** — 기사 제목을 질의로 뉴스 검색을 건다.
     고정된 피드 풀만 뒤지면 증인이 거의 항상 0건이라(9장 실측) 이 단계가 없으면 검수가 동작하지 않는다.

   | 검색 백엔드 | 키 | 증인 본문 |
   |---|---|---|
   | `naver` | 필요 | `originallink`(원문 URL)와 실제 발췌를 준다. **권장** |
   | `google_news` | 불필요 | 링크가 암호화 리다이렉트라 본문을 못 가져온다. 제목 대조까지만 가능 |

2. **순위·시효 판정** — 증인 후보와 검증 대상의 **단어 단위 TF-IDF 코사인 유사도**를 계산하고,
   `cluster_threshold`(기본 0.35) 이상만 같은 사건으로 본다.
   2단계와 달리 문자 n-gram을 쓰면 안 된다 — 같은 사건과 '같은 기업이 나오는 다른 사건'을 구분하지 못한다(9장).
   여기에 **시효 검사**를 더한다. 대상 기사와 `peer_max_age_hours`(기본 96) 넘게 떨어진 보도는 제외한다.
   보도가 적은 사건일수록 검색이 오래된 유사 기사를 끌어오고, 그게 독립 출처로 잡히면 교차검증이 조용히 오염된다.
3. **증인 본문 확보** — 상위 `max_peer_fetch`(4)건의 원문을 실제로 읽어온다.
   제목만으로는 `key_facts` 의 수치를 검증할 수 없다. 애그리게이터 링크는 본문을 못 가져오므로 건너뛴다.
4. **독립 출처 카운트** — 세 겹으로 거른다.
   - **출처 키가 같으면 제외.** 키는 도메인이 기본이지만, 본문에 통신사 바이라인(`(서울=연합뉴스)` 등)이
     보이면 그 통신사명을 쓴다. 연합뉴스 기사를 세 매체가 받아쓴 것은 도메인이 셋이어도 취재원은 하나다.
   - 이미 채택한 증인과 유사도 ≥ `duplicate_threshold`(0.9)면 제외 (글자까지 같은 전재)
   - `max_cluster_peers`(6) 까지만
5. **LLM 대조 검증** — 검증 대상의 `key_facts`와 증인 기사들의 제목·본문을 함께 넣고 사실별 판정을 받는다.

```json
{
  "fact_checks": [
    {"fact": "...", "status": "supported|contradicted|unverified",
     "evidence": "근거 출처명", "note": ""}
  ],
  "overall": "일관성 총평"
}
```

6. **신뢰도 산출**

```
independence = min(독립_도메인_수 / 3, 1.0)          # 3개 출처면 만점
agreement    = supported / max(총_fact_수, 1)
penalty      = 0.4  if contradicted > 0  else 0
confidence   = clamp(0.55 × independence + 0.45 × agreement − penalty, 0, 1)
```

| verdict | 조건 |
|---|---|
| `DISPUTED` | `contradicted` 사실이 1개 이상 |
| `VERIFIED` | 독립 출처 ≥ 3 **그리고** confidence ≥ 0.7 |
| `LIKELY` | 독립 출처 ≥ 2 **그리고** confidence ≥ 0.5 |
| `SINGLE_SOURCE` | 그 외 |

**요구사항**

| ID | 내용 |
|---|---|
| R4-1 | 증인이 하나도 없으면 LLM 호출을 건너뛰고 즉시 `SINGLE_SOURCE` 판정. 물어볼 근거 자체가 없다(비용 방어) |
| R4-2 | 판정 근거(매칭 도메인 목록, 사실별 판정)를 보존해 발행물에 표시하고 사후 검증을 가능하게 한다 |
| R4-3 | `min_confidence_to_publish`(기본 0.0) 미만은 발행에서 제외한다 — 기본값은 "전부 발행하되 등급 표시" |
| R4-4 | `DISPUTED`는 제외하지 않고 **경고 배지를 달아 발행**한다. 상충 자체가 독자에게 정보다 |
| R4-5 | 증인 검색은 켜고 끌 수 있어야 한다 (`verify.corroboration_search.enabled`) |
| R4-6 | 검증하지 못하는 것보다 **잘못 검증하는 쪽이 훨씬 나쁘다**. 임계값은 거짓 교차검증이 생기지 않는 쪽으로 잡는다 |
| R4-7 | 증인의 본문을 실제로 확보한다. 제목만으로 팩트체크하면 수치 주장이 영원히 `unverified` 로 남는다 |
| R4-8 | 증인은 대상 기사와 같은 시간대의 보도여야 한다. 발행시각을 모르는 기사는 버리지 않는다(R1-4 와 같은 원칙) |
| R4-9 | 통신사 전재는 도메인이 달라도 독립 출처 1곳으로 센다 |

**산출** `verified`

---

### 3.5 [5단계] 발행 — `publish`

**목적** Discord 채널에 뉴스레터를 띄운다.
**채널** `publish.targets` 에 적은 곳으로 동시에 보낸다. 셋 다 봇 상주 프로세스가 필요 없다.

| 채널 | 방식 | 필요한 값 | 길이 제한 |
|---|---|---|---|
| `discord` | Incoming Webhook | `DISCORD_WEBHOOK_URL` | embed 10개 / 합계 6,000자 |
| `slack` | Incoming Webhook (Block Kit) | `SLACK_WEBHOOK_URL` | 블록 50개 / section 3,000자 |
| `telegram` | Bot sendMessage (HTML) | `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID` | 메시지 4,096자 |

채널 하나가 실패해도 나머지는 계속 보낸다. **한 곳이라도 성공해야** 발행 이력에 기록한다 —
전부 실패했는데 기록하면 다음 실행에서 그 기사들이 '이미 발행함' 으로 걸러져 영영 나가지 못한다.

**메시지 구조**
- 헤더 메시지 1건 — 날짜, 총 건수, 등급 분포, 수집/선별 통계
- 기사별 Embed — 제목(링크) · 요약 bullet · `신뢰도` 필드 · `교차 출처` 필드 · footer(매체명 · 발행시각)
- Embed 색상 — `VERIFIED` 초록 / `LIKELY` 파랑 / `SINGLE_SOURCE` 회색 / `DISPUTED` 빨강

**요구사항**

| ID | 내용 |
|---|---|
| R5-1 | Discord 제약 준수 — 메시지당 embed 10개, embed 합계 6,000자, description 4,096자, title 256자. 초과 시 분할 전송 |
| R5-2 | 429 응답 시 `retry_after`만큼 대기 후 재시도(최대 3회). 연속 전송 사이 최소 간격 유지 |
| R5-3 | `dry_run: true`이면 네트워크 전송 없이 콘솔 + 파일로 출력 |
| R5-4 | 실행마다 `output/<run_id>.json`(전체 상태)과 `output/<run_id>.md`(사람이 읽는 뉴스레터)를 남긴다 |
| R5-5 | Publisher 인터페이스 + 레지스트리로 채널을 추가할 수 있게 한다 |
| R5-6 | 여러 채널에 동시에 보낼 수 있어야 하고, 한 채널의 실패가 다른 채널을 막지 않아야 한다 |

**산출** `publish_result`

---

### 3.6 발행 이력 — 같은 소식이 며칠 연속 나가는 것 방지

기사 ID 만 기억해서는 부족하다. 같은 사건을 다룬 '다른 기사'가 다음 날 올라오면
ID 가 달라 그대로 통과하기 때문이다. 그래서 사건 텍스트도 함께 저장하고 두 곳에서 거른다.

| 시점 | 기준 | 효과 |
|---|---|---|
| 2단계 선별 | 기사 ID 완전 일치 | 같은 기사 재발행 차단 (비용 0) |
| 4단계 검수 | 지난 발행분과의 사건 유사도 | 같은 사건의 후속·중복 보도 차단 |

**기록 시점은 실제 발행에 성공했을 때뿐이다.** `dry_run` 실행이 이력을 남기면
정작 진짜 발행할 때 전부 '이미 발행함'으로 걸러져 빈 뉴스레터가 나간다.
(`history.record_on_dry_run` 으로 바꿀 수 있지만 기본값은 `false`.)

저장소는 SQLite(`state/published.db`)이며 `keep_days`(90) 이후 자동 정리된다.

---

### 3.7 로컬 GUI

CLI 만으로는 매번 명령을 치고 yaml 을 열어야 해서 일상적으로 쓰기 번거롭다.
`app.py` 가 Flask 로 로컬 서버를 띄우고 브라우저 콘솔을 제공한다.

| 탭 | 기능 |
|---|---|
| 실행 | 단계 선택·연습 실행·발행 승인 토글, 진행 단계 표시, 로그 실시간 중계(SSE), 결과 카드 |
| 설정 | 관심사 키워드, 소스 on/off, 임계값, LLM 백엔드·모델, 발행 채널, **API 키** |
| 이력 | `output/*.json` 목록과 상세 |

**설계 제약**

| ID | 내용 |
|---|---|
| G-1 | `127.0.0.1` 에만 바인딩한다. 로컬 파일과 `.env` 비밀값을 다루므로 외부에 열지 않는다 |
| G-2 | 설정 저장은 ruamel 라운드트립으로 **yaml 주석을 보존**한다 |
| G-3 | API 키는 `.env` 에 저장하고 **값을 브라우저로 돌려보내지 않는다.** 설정 여부와 끝 4자리만 내보낸다 |
| G-4 | 키 저장은 프로세스 환경변수에도 즉시 반영해 서버 재시작 없이 실행할 수 있게 한다 |
| G-5 | 비밀값은 로그에 남기지 않는다. 바뀐 키 **이름만** 기록한다 |
| G-6 | 실행은 한 번에 하나. 진행 중에는 실행 버튼을 잠근다 |
| G-7 | CLI 와 GUI 는 같은 실행 경로(`runner.run_stages`, `graph.build_pipeline`)를 쓴다 |

---

## 4. 설정

| 파일 | 역할 |
|---|---|
| `.env` | 비밀값 — `ANTHROPIC_API_KEY` / `OPENAI_API_KEY` / `DISCORD_WEBHOOK_URL` / `NAVER_CLIENT_ID`·`NAVER_CLIENT_SECRET` |
| `config/config.yaml` | 실행 전반 — LLM provider·model, 동시성, lookback, dry_run, 검수 임계값 |
| `config/sources.yaml` | 수집 소스 목록 |
| `config/interests.yaml` | 관심사·키워드·선별 임계값 |

**LLM 프로바이더 전환** — `config.yaml`의 `llm.provider` 를 바꾸면 어댑터가 해당 백엔드를 쓴다.
키가 없으면 시작 시점에 명확한 에러로 중단한다.

| provider | 필요한 키 | 비고 |
|---|---|---|
| `anthropic` | `ANTHROPIC_API_KEY` | |
| `openai` | `OPENAI_API_KEY` | |
| `gemini` | `GOOGLE_API_KEY` | |
| `local` | 보통 없음 | OpenAI 호환 엔드포인트. `llm.base_url.local` 필수 (Ollama·LM Studio·vLLM) |
| `mock` | 없음 | 규칙 기반. 키 없이 전 구간을 돌려보는 용도 |

---

## 5. 비기능 요구사항

| ID | 항목 | 기준 |
|---|---|---|
| NFR-1 | 실행 시간 | 소스 10개 / 선별 8건 기준 5분 이내 |
| NFR-2 | 비용 | 1회 실행당 LLM 호출 = 선별건수(요약) + 클러스터 보유 건수(검증) ≤ 16회 |
| NFR-3 | 내결함성 | 단일 소스·단일 기사 실패가 파이프라인을 중단시키지 않음 |
| NFR-4 | 관측성 | 단계별 건수·소요시간을 `stats`에 기록하고 콘솔에 요약 출력 |
| NFR-5 | 재현성 | 모든 실행 상태를 `output/<run_id>.json`으로 저장 |
| NFR-6 | 예의 | User-Agent 명시, robots.txt 준수, 요청 간 지연 |
| NFR-7 | 저작권 | 원문 전재 금지 — 요약과 원문 링크만 발행 |

---

## 6. 디렉토리 구조

```
NEWS AGENT/
├─ PRD.md
├─ README.md
├─ requirements.txt
├─ .env.example
├─ run.py                      # CLI 엔트리포인트
├─ app.py                      # 로컬 GUI 서버 (Flask)
├─ web/                        # GUI 프런트엔드 (index.html · style.css · app.js)
├─ config/
│  ├─ config.yaml
│  ├─ sources.yaml
│  └─ interests.yaml
├─ src/newsagent/
│  ├─ config.py                # 설정 로딩·검증
│  ├─ models.py                # Article / Brief / VerifiedBrief
│  ├─ state.py                 # NewsletterState
│  ├─ llm.py                   # provider 어댑터 + JSON 구조화 호출
│  ├─ graph.py                 # LangGraph 조립 (체크포인터·재시도·승인 인터럽트)
│  ├─ search.py                # 교차검증용 기사 검색 (naver / google_news)
│  ├─ store.py                 # 발행 이력 (SQLite)
│  ├─ secrets.py               # .env 읽기·쓰기 (GUI 키 입력)
│  ├─ runner.py                # 단계 실행 (CLI·GUI 공용)
│  ├─ collectors/              # base / rss / api / crawl
│  ├─ nodes/                   # collect / curate / research / verify / publish
│  ├─ publishers/              # base / discord / slack / telegram
│  └─ utils/                   # text(정규화·TF-IDF) / http / extract
├─ tests/
├─ state/                      # published.db · checkpoints.db (자동 생성)
└─ output/
```

---

## 7. 마일스톤

| # | 범위 | 완료 기준 |
|---|---|---|
| M1 | 뼈대 + 모델 + 설정 로더 | `python run.py --check` 가 설정을 읽고 검증 결과를 출력 |
| M2 | 1단계 수집 | 실제 피드에서 기사 수집·중복 제거 확인 |
| M3 | 2단계 선별 | LLM 없이 점수/선별 동작 |
| M4 | 3단계 취재 | 본문 추출 + 구조화 요약 (mock LLM으로도 통과) |
| M5 | 4단계 검수 | 클러스터링·교차검증·등급 산출 |
| M6 | 5단계 발행 | dry-run 출력 → 실제 Webhook 전송 |
| M7 | E2E + 문서 | 전 구간 1회 완주, README 작성 |

---

## 8. 리스크 / 오픈 이슈

| 리스크 | 대응 |
|---|---|
| 사이트별 본문 추출 실패 | 스니펫 폴백 + 실패율을 stats에 노출 |
| 통신사 전재로 인한 거짓 교차검증 | **구현 완료** — 도메인 기준 카운트 + 채택 증인과 유사도 ≥ 0.9 면 동일 원본으로 보고 제외 |
| 한국어 TF-IDF 정확도 | 2단계는 문자 n-gram, 4단계는 단어 단위 + 경량 어간화(5자 이상 단어는 앞 4자)로 대응. 필요 시 임베딩 API 백엔드 추가 |
| 증인 검색 의존 | 검색이 막히면 4단계가 corpus 만으로 돌아가 대부분 `SINGLE_SOURCE` 가 된다. 잘못된 검증이 아니라 '검증 못 함'으로 표시되므로 안전 방향 실패다 |
| LLM JSON 파싱 실패 | 재시도 1회 + 폴백 경로 |
| Discord rate limit | 429 백오프 + 배치 분할 |
| 크롤링 차단 | robots.txt 준수, 실패 시 해당 소스 비활성 권고 로그 |

---

## 9. 구현 중 확정된 설계 결정 (실측 근거)

명세만으로는 판단할 수 없어 실제 데이터로 측정한 뒤 정한 것들이다.
모두 328건 규모의 실제 수집 코퍼스에서 잰 값이다.

### 9.1 키워드에 IDF 가중이 필요했던 이유

가중 없이 매칭 수만 세자 선별 1위가 **"북구, 소상공인 생성형 인공지능(AI) 활용 디지털 교육 프로그램"** 이 됐다.
제목에 `AI`·`인공지능`·`생성형` 세 개가 박혔다는 이유만으로, 같은 날의 `HBM`·`TSMC` 기사를 이겼다.

코퍼스에서 잰 실제 IDF:

| 키워드 | IDF |
|---|---|
| `AI` | 0.20 |
| `반도체` | 0.31 |
| `인공지능` | 0.32 |
| `데이터센터` | 0.56 |
| `HBM`·`TSMC`·`GPU` | 1.00 |

`AI` 한 번 맞히는 것과 `HBM` 한 번 맞히는 것은 5배 다른 정보량이다. 이걸 반영하자 순위가 정상화됐다.

### 9.2 벡터 점수 정규화 — 열 단위, 최대값 기준

char n-gram 코사인 원값 분포: `p50=0.009`, `p95=0.043`, `max=0.108`.
가중치 0.4를 곱하면 기여도가 0.04라 하이브리드가 사실상 키워드 전용이었다.

- **전체 기준으로 정규화하면 안 된다** — 열별 최대값이 0.108(AI) vs 0.061(개발자도구)로 달라 프로필이 긴 관심사가 구조적으로 유리해진다.
- **p98 로 자르면 안 된다** — 상위 2%가 전부 1.0으로 붙어, 정작 순위를 가려야 할 최상단에서 벡터가 무력해졌다.

→ **관심사(열)별 최대값**으로 나눈다. 상위 8건의 벡터 점수가 `1.00 / 0.93 / 0.90 / 0.85 / 0.82 / 0.68 / 0.65 / 0.61` 로 제대로 퍼졌다.

### 9.3 4단계는 문자 n-gram을 쓰면 안 된다

같은 벡터화를 4단계에 쓰자 **같은 사건**과 **같은 기업이 나오는 다른 사건**을 전혀 구분하지 못했다.

기준 기사: `삼성전자·SK하이닉스, 한전 '전기료 25조 선납' 제안 거절`

| 벡터화 | 같은 사건 | 같은 기업·다른 사건 | 변별폭 |
|---|---|---|---|
| `char_wb(2,4)` | 0.225 | 0.227 | **−0.001** |
| `char(2,3)` | 0.198 | 0.174 | +0.024 |
| **`word(1,1)`** | **0.415** | **0.129** | **+0.286** |
| `word(1,2)` | 0.193 | 0.104 | +0.090 |

두 기사 모두 '삼성전자·SK하이닉스·반도체'라는 문자열을 공유하기 때문에 문자 n-gram으로는 갈 수 없다.
단어 단위 TF-IDF는 IDF가 범용 토큰의 무게를 깎고 `전기료`·`선납`·`한전` 처럼 그 사건에만 나오는 토큰을 살려 깨끗하게 갈랐다.
한국어 조사는 **5자 이상 단어를 앞 4자로 자르는 경량 어간화**로 흡수한다(`삼성전자가` → `삼성전자`).

### 9.4 고정 피드 풀만으로는 교차검증이 성립하지 않는다

피드는 '오늘 뜨는 기사'를 줄 뿐 '내가 검증하려는 그 사건을 다룬 기사'를 주지 않는다.
실측에서 선별 8건 **전부 증인 0건**이 나왔다. 임계값을 낮추는 건 답이 아니다 — 무관한 기사가
서로의 '독립 출처'로 잡혀 **허위 교차검증**이 되고, 그건 검증하지 못하는 것보다 나쁘다.

그래서 사건별 뉴스 검색을 추가했다. 같은 코퍼스에서 다시 측정한 결과:

| 기사 | 검색 결과 | 확보한 독립 출처 |
|---|---|---|
| 삼성전자·SK하이닉스, 한전 전기료 선납 거절 | 20건 | **6곳** (news1·비즈니스포스트·연합뉴스TV·천지일보·국제신문·자본시장) |
| 오픈AI 불량 에이전트 루비젬스 해킹 | 3건 | 0곳 (단독 보도) |
| 삼성전자 日 패키징 R&D 거점 | 19건 | 0곳 (단독 보도) |

널리 보도된 사건과 단독 보도가 정확히 갈렸다. 이것이 4단계의 목적 그 자체다.

### 9.5 구현 중 잡은 버그

| 증상 | 원인 | 조치 |
|---|---|---|
| 3단계에서 기사 2건이 `AttributeError` 로 유실 | `find_all()` 결과를 순회하며 부모를 `decompose()` 하면 뒤따르는 자식 Tag 의 `attrs` 가 None 이 된다 | 순회 중 `node.decomposed` 확인 |
| 요약에 `&nbsp;` 가 그대로 노출 | HTML 엔티티를 해제하지 않음 | `clean_text` 에서 태그 제거 → 엔티티 해제 → 잔재 태그 재제거 |
| 같은 소식이 뉴스레터에 두 번 게재 | 선별 단계는 기사 단위라 같은 사건 중복을 모름 | 4단계 진입 시 같은 사건 병합(대표 1건만 발행) |
| 구글뉴스 기사 요약이 제목 재탕 | 애그리게이터 링크에서 본문을 못 가져옴 | 소스 `role` 도입 — 애그리게이터는 증인 전용 |

### 9.6 증인 기사에 본문이 없었다 (v1.2 에서 해결)

4단계가 돌아는 갔지만, LLM 이 받는 대조 근거가 무엇인지 실제로 까보니 이랬다.

```
yonhapnewstv.co.kr   snippet=41자  content=0자
  실제내용: '삼성전자·SK하이닉스, 한전 '25조 전기료 선납' 제안 거절 연합뉴스TV'
electimes.com        snippet=43자  content=0자
  실제내용: '한전 '전기료 25조 선납' 무산...삼성전자·SK하이닉스 모두 거절 전기신문'
```

증인 20건 전부 `content=0자`. 즉 **헤드라인과 매체명만으로 팩트체크**하고 있었다.
신뢰도 공식에서 `agreement` 가 45% 를 차지하는데 그 45% 가 제목 대조로 계산된 셈이다.
"삼성·SK가 거절했다"는 큰 줄기는 제목으로 확인되지만, `key_facts` 의
"삼성 약 20조원, SK하이닉스 약 5조원" 같은 수치는 제목에 없어 영원히 `unverified` 였다.

원인은 검색 백엔드였다. 구글뉴스 RSS 는 링크가 암호화 리다이렉트(실측 580KB 자바스크립트 셸)라
원문을 받아올 수 없고 스니펫도 제목의 반복이다. 그래서 `originallink` 와 실제 발췌를 주는
네이버 검색 API 백엔드를 추가하고, 상위 증인의 본문을 직접 가져오는 단계를 넣었다.

### 9.7 근접중복 필터로는 통신사 전재를 못 잡는다

v1.1 은 '채택한 증인과 유사도 0.9 이상이면 전재로 보고 제외'로 대응했다.
매체가 제목을 조금씩 고쳐 싣는 실제 전재 형태로 재보니 통하지 않았다.

| 전재본 쌍 | 유사도 |
|---|---|
| 0-1 | 0.340 |
| 0-2 | 0.226 |
| 1-2 | 0.152 |

전부 0.9 을 한참 밑돈다. 같은 사건이라 대상 기사와는 잘 묶이지만(0.63~1.0),
서로 간에는 문장이 달라 근접중복으로 잡히지 않는다.
결국 도메인 3개 = 독립 출처 3곳으로 세어 신뢰도가 부풀려진다.

그래서 본문의 통신사 바이라인을 직접 읽는다.
`(서울=연합뉴스)`, `[세종=뉴시스]`, `저작권자 ⓒ 연합뉴스`, `(Reuters)` 를 잡아
그 값을 도메인 대신 '독립 출처 키'로 쓴다. 테스트는 바이라인이 있을 때 1곳,
없을 때 3곳으로 갈리는 것을 대조군과 함께 검증한다.

### 9.8 수집 단계 제목 중복제거가 증인을 먼저 지우고 있었다

제목 정규화 일치를 전역으로 적용하니, 매체가 다른데 제목이 같은 기사가 지워졌다.
실측 400건 중 4개 그룹이 해당했다.

```
· "내년 인공지능전공 신설... 사람 중심 AI 교육으로 경쟁력 키울 것"
  살아남음=v.daum.net  버려짐=['donga.com']
· '가스 압력 자동 조절' 반도체 장비 무관세 적용
  살아남음=yna.co.kr   버려짐=['stock.mk.co.kr']
```

매체가 다른데 제목이 같다는 건 중복이 아니라 **같은 사건을 각자 보도했다**는 뜻이고,
그게 정확히 4단계가 찾는 것이다. 제목 중복 판정을 같은 도메인 안으로 좁혔다.
영향 자체는 작지만(400건 중 4건), 지워지는 대상이 하필 교차검증 후보라 방향이 나빴다.

### 9.9 단독 기획 보도의 SINGLE_SOURCE 판정과 시효·도메인 방어 실측 (2026-09-15)

실측 실행 `20260915_143832` (831건 수집 → 8건 선별 → 6건 발행) 분석:
- **3건 검증 완료 (`VERIFIED`, 독립 출처 3~6곳)**: 엔비디아 RTX Pro 5500, d-Matrix NVLink Fusion, 젠슨 황 속도조절론 반대
- **3건 단일 출처 (`SINGLE_SOURCE`, 증인 0곳)**:
  - 블로터 (CLSA 글로벌 투자은행 리포트 단독 입수·분석 기사)
  - 아이뉴스24 (AI 인프라 서밋 + LA 올인 서밋 기자 종합 기사)
  - ZDNet Korea (IP-SoC 데이 코리아 현장 취재 단독 속보)

**검증 방어 메커니즘 실측 결과**:
1. **자사 도메인 배제**: 검색 결과 1위로 잡힌 원문 매체 본인 기사를 증인으로 인정하지 않고 차단 (`candidate.domain == target.domain`).
2. **시효 초과 차단**: ZDNet 기사의 경우 검색된 타사 유사 기사들이 2.5달 전(7월 1일) SAFE 포럼 기사였으나, `peer_max_age_hours: 96시간` 필터가 과거 기사를 통한 팩트체크 왜곡 오염을 100% 차단.
3. **결론**: 단독 기획 보도가 `SINGLE_SOURCE`로 남는 것은 시스템의 결함이 아니라, "허위 교차검증을 생성하지 않는다"는 에이전트의 핵심 안전장치가 실전에서 완벽히 작동하고 있음을 입증.

---

## 10. 저작권 및 라이선스 (Proprietary Rights)

Copyright (c) 2026 cjwmanelf. All rights reserved.

- 본 프로젝트의 모든 소스 코드 및 기술 문서는 원저작자(`cjwmanelf`)의 독점적 저작물입니다.
- **허용**: 비상업적 개인 연구 목적의 원본 소스 열람 및 로컬 실행
- **금지**: 원저작자의 사전 서면 동의 없는 **무단 수정, 편집, 2차적 저작물 작성, 수정된 버전의 Fork/재배포 및 영리 목적의 상업적 이용 일체 금지**
- 법적 고지 및 세부 약관은 프로젝트 루트의 [LICENSE](LICENSE) 파일을 참조하십시오.

